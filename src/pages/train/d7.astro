---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CognitiveFlexibility from '../../components/training/CognitiveFlexibility.tsx';
import type { Locale } from '../../i18n/ui';

const locale: Locale = 'en';
---

<BaseLayout
  locale={locale}
  title="D7 Cognitive Flexibility Training — ForgeGaming"
  description="Train your cognitive flexibility with 3D magic chessboard rule-switching"
>
  <CognitiveFlexibility client:only="react" locale={locale} />
</BaseLayout>
