---
import BaseLayout from '../../layouts/BaseLayout.astro';
import DecisionSpeed from '../../components/training/DecisionSpeed.tsx';
import type { Locale } from '../../i18n/ui';

const locale: Locale = 'en';
---

<BaseLayout
  locale={locale}
  title="D8 Decision Speed Training — ForgeGaming"
  description="Train your decision speed with multi-target resource allocation in 3D immersive environments"
>
  <DecisionSpeed client:only="react" locale={locale} />
</BaseLayout>
