# AGENTS.md — 项目约定与工作规范

## 项目概述

正在开发一个**电竞能力训练平台**——网页内能力训练网站，面向竞技类游戏爱好者（FPS/MOBA），训练底层能力维度而非游戏内操作。核心体验是让用户可见地感知自己能力的增长。

## 开发流程

按正规产品流程推进，分为以下步骤，每步确认后再进入下一步：

1. 需求定义 ✅
2. 竞品与市场调研 ✅
3. 产品规划 ✅
   3.5. 各维度训练设计 ✅
4. 信息架构与交互设计 ✅
5. 视觉设计 ✅
6. 技术方案 ✅
7. 开发实现
8. 测试与上线

## 关键约定

### 开发规范文档（进入开发阶段后必读）

- **`docs/开发规范.md`**：编码规则（TypeScript/React/Astro/Three.js）、代码审查清单、安全规则、反模式清单。写代码、提交代码前必读
- **`docs/开发流程.md`**：Git分支策略、提交规范、TDD 7阶段流程、测试要求、CI/CD、部署流程。初始化项目、写功能、提交、测试、部署时必读

### 开发精华原则（始终遵守）

1. 先写测试再写实现（TDD）
2. 每次提交前按审查清单自检
3. 敏感操作（部署、数据库变更、破坏性git操作）必须用户确认
4. 构建或测试中保持沟通，不静默执行
5. 密钥放.env.local，禁止提交到git
6. 多语言修改必须同步en/zh
7. Three.js对象切换时必须dispose
8. 不过度设计，只做当前需求

### 文档维护

- **每次确认信息、通用原则、或完成一个设计节点后，立即更新 `产品规划文档.md`**
- 文档是单一事实来源，所有决策的最终记录都在那里
- 版本号和进度状态随更新递增

### 开发日志（进度追踪）

- **`log/` 目录**：按天创建开发日志（如 `log/2026-08-27.md`），简洁概括型，记录重要开发节点、已完成/待办、遇到的问题、关键决策
- 新会话开启时先读 `log/` 目录下最新日志，了解当前进度，决定从何继续
- 同时维护 `log/技术文档.md`：记录系统架构、模块结构、数据模型、关键接口等技术细节

### 沟通方式

- 所有输出使用中文
- 每个设计决策需用户确认后才算锁定，未确认的标注"待确认"
- 讨论时先提出方案，用户拍板后记录

### 训练设计9条通用原则

1. 全屏浮窗运行，零广告零干扰
2. 渐进式难度为主（从易到难，直到失败）——但认知类维度（1,6,7,8）可不用渐进式和失败机制，用固定难度+纯计分；操作类维度（2,3,4,5）保留渐进式+失败。具体每个维度具体设计
3. 测试与训练共用同一套模块
4. 多组结构（每组间显示对比）
5. 组间实时反馈（数据对比+教练提示）
6. 每组与个人最佳对标
7. 突破个人最佳触发特效
8. 每维度除主指标外找1-2个可改善子指标展示进步感
9. 全维度3D沉浸式环境（Three.js）——所有训练在3D中进行，鼠标灵敏度可配置（Pointer Lock API）

### 已锁定的产品决策

- 只分MVP和二期，不分三期
- 不做移动端适配
- 不做公开排行榜（防刷分）
- 社交用单向关注模型（非互加好友）
- 测试免登录免费，训练需登录
- 免费为主+广告，二期推付费功能
- 8个能力维度（已取消操作耐力/APM）
- 偏好设置（FPS/MOBA/全部）可在设置中随时更改
- 全部8个维度均使用Three.js 3D环境（不混用2D/3D）
- 瞄准训练用3D人形靶（头/身分区=爆头训练），单发模式
- 鼠标灵敏度可配置（Pointer Lock API）
- 通用维度（4,8）做FPS+MOBA双版本；维度1、7为单一通用版；FPS专属维度（2,3）只做FPS版；MOBA专属维度（5,6）只做MOBA版
- FPS版用第一人称摄像机，MOBA版用等距俯视摄像机，维度1、7用固定正面3D摄像机
- FPS版主题=射击靶场，MOBA版主题=守塔防御，维度1主题=魔法球，维度7主题=魔法棋盘（均游戏无关）
- 不做压枪训练（太游戏专项），不做狙击训练（已被维度2单发模式覆盖）
- 维度5去掉遮挡机制（与维度6重叠），遮挡/消失/回忆全部归维度6
- 维度8用三种目标类型（普通/高攻/坦克）+倒计时机制，不用颜色=优先级
- 维度7改为单一通用版（棋盘+灯设计），不做FPS/MOBA双版本——实际游戏场景特质太强反而限制通用认知灵活性训练
- 维度7无渐进式难度、无失败机制，固定参数+2分钟计时+纯计分（同维度1思路）
- 各维度失败条件可不同，不强制统一"3次miss"；认知类维度（1,6,7,8）倾向无失败纯计分，操作类维度（2,3,4,5）保留渐进式+失败
- MVP一次性开发全部10个模块，然后逐个调校
- 视觉风格=熔炉锻造（Forge）：深炭灰底(#161618) + 烈焰橙(#FF4500) + 金色(#FFD700)，工业硬核感
- 深色方案（非浅色），理由：3D训练场景衔接+突破特效+电竞用户审美预期+长时间训练视觉舒适
- 首页双按钮：左"免费测试我的能力"（橙底，免登录），右"开始系统练习"（金边，注册引导）
- 首页首屏背景：左半FPS角色动态剪影 + 右半MOBA角色动态剪影，半透明叠加+底部渐变过渡
- 首页取消广告位，改为10个主流竞技游戏标签（Valorant/CS2/Apex/OW2/PUBG/R6/英雄联盟/Dota2/王者荣耀/Wild Rift），利于SEO
- 训练中心为登录后默认首页
- 全站默认语言为英语，同时做 /zh/ 中文版本
- 多语言同步规则：任何一个语言版本的修改，另一语种必须同步修改
- 技术栈：Astro（静态页SSG，零JS，SEO最优）+ React岛屿（交互页面）+ Three.js（3D训练）+ Supabase（后端）+ Vercel（部署）
- 多语言策略：URL路由，.com默认英语，输入/zh/才进入中文，不做浏览器语言自动检测跳转
